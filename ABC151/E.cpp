#include <bits/stdc++.h>
#include <algorithm>

#define rep(i, n) for(int i = 0; i < n; i++)
using namespace std;
using ll = long long;

void vprint(vector<ll>);

int main() {
  int N, K;
  cin >> N >> K;
  vector<ll> a(N);
  rep(i, N) { cin >> a[i]; }

  
}
